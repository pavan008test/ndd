This folder is to store KA commands and its corresponding scripts. If any script are modified and if that script has corresponding KA command then update that KA_cmd_*.txt file so that it will be matching with main script.
