[Unit]
Description=Tmpfs for /tmp
DefaultDependencies=no
Conflicts=umount.target
Before=local-fs.target sysinit.target umount.target
After=swap.target

[Mount]
What=tmpfs
Where=/tmp
Type=tmpfs
Options=defaults,mode=1777,strictatime,nosuid,nodev

[Install]
WantedBy=local-fs.target
